[project]
name = "week3-ai-poc"
version = "0.1.0"
description = "Week 3 AI POC — Retrieval & RAG: an 'ask my documents' app for Recipes & Food"
requires-python = ">=3.10"
readme = "README.md"

[tool.setuptools.packages.find]
include = ["app*"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
