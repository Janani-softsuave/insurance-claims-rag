# ---- Core / config ----
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# ---- Embeddings (bi-encoder) + reranker (cross-encoder) ----
sentence-transformers>=3.0

# ---- Vector database (HNSW) ----
chromadb>=0.5.5

# ---- Document loading ----
pypdf>=4.2

# ---- Grounded generation (Gemini) + structured output ----
google-genai>=0.3.0
instructor>=1.6.0
jsonref>=1.1.0  # required by instructor for Gemini structured output

# ---- UI ----
streamlit>=1.35

# ---- Interfaces ----
fastapi>=0.111
python-multipart>=0.0.9  # required by FastAPI for file upload (UploadFile)
uvicorn[standard]>=0.30
typer>=0.12
rich>=13.7
